printf("t");

print(q, len(n) = 1);

printf("t");

print(m, len(n));

printf("cdn", cnt);

ls(len(n), len(n) = 1);

printf("LSHIFTt");

printaq();

addBinary(a, mc);

printf("A=A-ME");

printaq();

if (a[0] = 1) {

q[len(n) 2] = 0;

addBinary(a, m);

} else

q[len(n) - 2]

printf("A=A+MC");

printaq();
